# גן מזור — גרסה 2

## מה עובד עכשיו
- אפליקציית ההורים: בית, לוח שנה, אלבומים, קהילה ועד.
- אזור ניהול נפרד ב־`admin.html`.
- עדכון צוות, מפגש בוקר, חוג, תזכורת ואבא/אמא של שבת.
- הוספת אירועים וסימון „אין גן”.
- יצירת אלבומים לתאריך.
- ניהול קופת ועד, הוצאות, סקר ורשימת כיבוד.
- שמירה מקומית בדפדפן במצב דמו.
- הכנה לחיבור Supabase ולכניסה בקישור חד־פעמי במייל.

## פתיחה במצב דמו
1. חלצו את ה־ZIP.
2. פתחו `index.html` לצפייה כהורה.
3. פתחו `admin.html` לניהול.
4. השינויים נשמרים באותו דפדפן ומופיעים במסך הבית לאחר רענון.

## חיבור ל־Supabase — לבצע יחד בשלב הבא
1. יוצרים פרויקט Supabase חדש.
2. מריצים פעם אחת בלבד את `supabase-schema.sql`.
3. יוצרים bucket פרטי בשם `gan-albums`.
4. מדביקים Project URL ומפתח ציבורי ב־`config.js`.
5. מגדירים כתובת אתר מורשית ב־Authentication.
6. יוצרים ומאשרים חברות בטבלת `memberships` לפי תפקיד: הורה, צוות, ועד או מנהל.

## חשוב
- לא משתמשים ב־service role key בדפדפן.
- לא הופכים את אלבום התמונות לציבורי.
- לא מריצים מחדש את קובץ הסכמה בלי בדיקה וגיבוי.
- החיבור המלא של טפסי הניהול ל־Supabase יבוצע לאחר יצירת הפרויקט.
